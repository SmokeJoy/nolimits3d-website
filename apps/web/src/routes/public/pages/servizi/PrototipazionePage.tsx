import { ServiceLayout } from './ServiceLayout';

/** `/servizi/prototipazione` -- see `ServiceLayout` for structure and placeholder policy. */
export function PrototipazionePage() {
  return <ServiceLayout />;
}
