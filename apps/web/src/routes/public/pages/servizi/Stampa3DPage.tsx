import { ServiceLayout } from './ServiceLayout';

/** `/servizi/stampa-3d` -- see `ServiceLayout` for structure and placeholder policy. */
export function Stampa3DPage() {
  return <ServiceLayout />;
}
