import { ServiceLayout } from './ServiceLayout';

/** `/servizi/piccole-serie` -- see `ServiceLayout` for structure and placeholder policy. */
export function PiccoleSeriePage() {
  return <ServiceLayout />;
}
