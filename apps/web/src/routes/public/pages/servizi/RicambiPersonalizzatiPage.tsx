import { ServiceLayout } from './ServiceLayout';

/** `/servizi/ricambi-personalizzati` -- see `ServiceLayout` for structure and placeholder policy. */
export function RicambiPersonalizzatiPage() {
  return <ServiceLayout />;
}
