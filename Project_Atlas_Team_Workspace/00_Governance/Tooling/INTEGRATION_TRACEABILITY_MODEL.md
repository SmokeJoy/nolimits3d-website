# Integration Traceability Model

## Flow
`Bible / Blueprint` -> `Linear Issue` -> `GitHub Branch` -> `Commits` -> `PR + Tests` -> `Review` -> `Merge`

## Requirements
- Every PR must reference a Linear Issue.
- Every Linear Issue must reference a Blueprint slice or PRD requirement.
- No orphan code.
