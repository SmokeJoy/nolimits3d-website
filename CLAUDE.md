# Project Atlas - Claude Compatibility Bridge

This file is a compatibility bridge. It does not assign governance authority.

The active repository instructions are in `AGENTS.md`. A Claude session working in this
repository must read and follow `AGENTS.md`, the Codex-native Development Framework v2.0.0,
and the binding Task Packet before doing any work.

The former Claude team in `.claude/agents/` is superseded by AD-010 and is not an active
execution path. Claude may participate only as an explicitly assigned implementer under
Atlas TPM. It may not claim the Codex Root, Chief Architect, Atlas TPM, Technical Review,
or final approval roles.

Non-negotiable boundaries remain unchanged:

- Jarvis is private to Andrea and requires server-side identity and capability controls.
- PrintFlow remains `Coming Soon`.
- The PC worker remains pull-only with no public inbound control port.
- `apps/legacy-web` remains the public fallback until an approved cutover.
- Production remains blocked by `BLK-BASE-001`.

Authoritative order and canonical validation commands are defined in `AGENTS.md`.
