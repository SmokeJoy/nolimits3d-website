# Architect Review PA-AR-M002-013

## Verdict

`REQUIRED CHANGES - WAVE B PARTIALLY REMEDIATED - NO MERGE - NO C1`

## Reviewed baseline

- Pull Request: `#5 - feat(M-002): Wave B - Token Foundation & Toggle Demo`
- Execution base: `5cf035c5b4ab3bb2515b987f3448abee811de727`
- Prior gate: `PA-AR-M002-012`
- Claude owner-correction commit: `749fbd60829c448bba388bfa723059b2b2956dcd`
- Unauthorized infrastructure commit: `956eacf48e109e37d92a3320953667196238ef77`
- Infrastructure revert head: `b48686158472d23da7cb0b4c07be07db98786abe`
- Revert-head CI: `29570118461 - SUCCESS`

## Accepted remediation

1. The playground now generates and applies the submitted Tailwind token utilities.
2. Runtime inspection confirms dark canvas `#101820`, white primary text, `36px` H1 typography and token-based `230ms` transitions.
3. The light-theme toggle now renders a white canvas with dark primary text.
4. The programmatic reduced-motion toggle now sets `data-motion="reduced"` and reduces transition duration to `0s`.
5. The unauthorized package and TypeScript configuration changes from `956eacf` were removed non-destructively by `b486861` without losing the valid runtime corrections from `749fbd6`.
6. Frozen installation, UI build, playground build, UI typecheck and the existing test suite pass on `b486861`.

These corrections resolve the original token-rendering blocker only. They do not close Wave B.

## Blocking findings

### 1. Reduced motion still violates the governed requirement

Both `prefers-reduced-motion: reduce` and the playground simulation resolve animation duration to `1ms` with a `-1ms` delay. `AND-28`, the Token Schema and `PA-AR-M002-012` require `0ms` and no animation.

Runtime verification on the current head produced:

- `transition-duration: 0s`;
- `animation-duration: 0.001s`;
- `animation-delay: -0.001s`.

Wave B cannot accept a deliberate `1ms` substitution for an explicit `0ms` contract.

### 2. Required automated verification is still absent

`packages/ui` still executes one Wave A placeholder render test. No automated test verifies:

- reduced-motion media or simulation behavior;
- dark/light semantic mappings;
- numeric contrast ratios;
- the complete foreground/background acceptance matrix.

A green generic CI run does not prove these Wave B requirements.

### 3. The governed Token Schema is still inconsistent with implementation

The Token Schema remains `PENDING ARCHITECT APPROVAL` and still maps light-mode `accent.primary.foreground` to `palette.white`, despite the binding correction in `PA-AR-M002-012` to use `palette.dark` in both themes.

The hover and warning/error/info colors also remain hardcoded in the semantic layer while their governed reference values are TBD. The corrected implementation must define candidate reference tokens first and map semantic tokens to those references.

### 4. The canonical evidence set is unchanged and invalid

The four Wave B evidence files still contain the defects rejected by `PA-AR-M002-012`:

- nonexistent implementation SHA `4c93f1e56b46cc3cbfe065ef8c8ca53e18f2d579`;
- nonexistent command `python contrast_audit.py`;
- invalid pnpm filter `apps/ui-playground`;
- untrue JSDOM-test claim;
- stale CI URL and incomplete implementation/final-head traceability;
- incorrect owner/executor provenance;
- missing accent-hover contrast pairings.

No Wave B evidence file was corrected by `749fbd6`, `956eacf` or `b486861`.

### 5. Process and scope violations occurred

The submitted handoff executed `git reset --hard` despite the explicit no-reset order in `PA-AR-M002-012`. No remote history loss was observed, but future correction and rollback must use normal commits or `git revert` only.

Commit `956eacf` modified `packages/ui/package.json` and `packages/ui/tsconfig.build.json`, both forbidden by `AND-28`, and reused the already closed `TSK-M002-CODEX-A`. Its source export also contradicted the approved package contract that runtime exports target `dist`. The commit has therefore been reverted.

Creating or populating `m002/wave-c1-primitives` does not authorize Wave C1. The supplied C1 prompt is rejected and must not be executed while Wave B remains open.

## Required remediation

Claude remains the exclusive owner of the remaining Wave B correction:

1. Change both reduced-motion blocks to `0ms`/no animation exactly as governed.
2. Add deterministic tests under `packages/ui/tests/**` for reduced motion and the complete numeric contrast matrix without adding dependencies or changing configuration.
3. Persist the `palette.dark` light accent-foreground decision in the canonical Token Schema.
4. Introduce candidate reference tokens for hover and status colors, then map the semantic tokens to those references.
5. Correct the same four canonical evidence files in place. Do not create versioned duplicates.
6. Record the real corrective implementation SHA, exact commands, UTC timestamps, exit codes, actual owner/executor and remote CI URLs for both implementation and final evidence heads.
7. Keep PR #5 draft and return it to the Chief Architect for a new Wave B gate.

Claude must not modify package manifests, lockfiles, CI, Vite, Vitest or TypeScript configuration. Codex has no active Wave B implementation task after the revert.

## Gate state

- PR #5: `DRAFT - CHANGES REQUIRED`
- AND-28: `IN PROGRESS - BLOCKED AT ARCHITECT REVIEW`
- Wave B: `OPEN`
- Wave C1 and local C1 work: `HOLD - DO NOT COMMIT OR PUSH`
- Waves C2/D/E and `/design-sync`: `BLOCKED`
- M-002: `OPEN`
- Jarvis: `UNCHANGED / OUT OF SCOPE`
- PrintFlow: `COMING SOON / OUT OF SCOPE`
